# Frontend

Vite React app for registering, logging in, adding transactions, and viewing spending insights.

Run locally:

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` to the FastAPI backend URL.
